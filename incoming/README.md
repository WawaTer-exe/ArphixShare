# Incoming videos

Upload MP4 files into this folder. The GitHub Actions importer checks approximately every 8 minutes, imports up to 20 files per run into `videos/`, creates playable HTML pages from `videos/example.html`, and refreshes the Hot Videos section on the homepage.

Only upload videos that you own or are authorized to publish.
